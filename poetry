pip install poetry

poetry init

Add requirements.txt:
for item in $(cat requirements.txt); do poetry add "${item}"; done
(python = "^3.11")

Once all dependencies have been added, you can install them using the command:
poetry install
